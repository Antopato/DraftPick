# Public URL of the backend. Leave empty in local dev (the Vite proxy is used).
# In production (Vercel/Netlify) set it to the Render backend URL, e.g.:
# VITE_API_URL=https://draftpick-api.onrender.com
VITE_API_URL=
